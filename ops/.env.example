# Printer configuration for Zebra ZD888T labels
PRINTER_MODE=local
PRINTER_LAYOUT=2across
PRINTER_DUPLICATE_SINGLE=True
PRINTER_HOST=192.168.1.50
PRINTER_PORT=9100
 
# API CORS (comma separated origins for UI)
API_CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080
